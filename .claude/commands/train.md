Run the training scripts for this project.

Run `uv run train` to train TrackNet (ball detector), then `uv run train-events` to train TCNEventNet (event classifier). Show the output of both commands. If the first fails, stop and report the error without running the second.
